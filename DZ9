Задание: 
1. Создать функцию наполнения списка путем ввода данных с клавиатуры.
2. Создать функцию которая будет перебирать список полученный из функции первого задания и вывести только числа из списка на экран.
3. Создать функцию которая будет генерировать числовые значения в кол-ве 200 шт., затем вывести наибольшее и наименьшее значение из списка.


1
# Создать функцию наполнения списка путем ввода данных с клавиатуры.

def add(text, int):
    print(text + int)

add("Он хоть на край земли отправиться готов.", "98765")
add("Он эльфов всех прогнал за дальний перевал.", "9,8,7,6,5")
text = "98765," "9,8,7,6"
print(text.split(","))



2
# Создать функцию которая будет перебирать список полученный из функции первого задания
# и вывести только числа из списка на экран.

my_list = ["Он хоть на край земли отправиться готов.", 98765]
list(filter(lambda x: type(x) is int, my_list))
result = list(filter (lambda x: type(x) is int, my_list))
print(result if result else "Цифры отсутствуют")


3
# Создать функцию которая будет генерировать числовые значения в кол-ве 200 шт.,
# затем вывести наибольшее и наименьшее значение из списка.

from random import randrange
def listnumbers():
    number = []
    for i in range(0,500):
        number.append(randrange(0,200))
    print(number)
    max_number = max(number)
    min_number = min(number)
    print("наибольшее число:",max_number)
    print("наименьшее число:",min_number)
listnumbers()
